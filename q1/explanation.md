# Explanation for Question 1

1. Who am I?
   - I verified the active Linux user account. The output showed that the session was running as codespace.

2. Groups
   - I checked the groups associated with the account. The user belonged to several groups that provide access to tools and services.

3. Shell verification
   - I confirmed the default shell in use. The environment reported /bin/bash.

4. Current directory
   - I verified the present working directory. The current directory was /.

5. Directory listing
   - I listed the contents of the filesystem to understand the workspace layout and confirm that common directories were present.

6. Network connectivity test
   - I tested external connectivity by requesting a website with curl. The command succeeded, so network access was available.
